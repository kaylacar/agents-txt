services:
  - type: web
    name: agents-txt-demo
    runtime: node
    buildCommand: npm i -g pnpm && pnpm install && pnpm -r build
    startCommand: node examples/live-demo/index.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        generateValue: true
